/** Absolute application URLs used for programmatic navigation. */
export const APP_ROUTES = {
  login: '/login',
  assessment: '/assessment',
  tasks: '/tasks',
  completedTasks: '/tasks/completed',
} as const;
